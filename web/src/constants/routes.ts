const ROUTES = {
  LANDING: '/',
  HOME: '/home',
  REGISTER: '/register',
  CALLBACK: '/callback',
  USER_NOT_FOUND: '/user-not-found',
};

export const { LANDING, HOME, REGISTER, CALLBACK, USER_NOT_FOUND } = ROUTES; 